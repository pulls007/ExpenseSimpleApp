package com.company.models.Split;

import com.company.models.User;

public class EqualSplit extends Split {

    public EqualSplit(User user) {
        /// we ll not set user amount by default;
        super(user);
    }
}
